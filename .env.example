# Server
PORT=3456

# Pi: auto-approve extension_ui_request confirm dialogs (e.g. "Run this command?") so tool execution
# does not block when the approval modal is not shown or user cannot respond.
# PI_AUTO_APPROVE_TOOL_CONFIRM=true
# Pi (Google Gemini) - required when using Pi with Gemini. Get key from https://aistudio.google.com/apikey
# Or run `pi` and `/login` for OAuth (no key needed).
# GEMINI_API_KEY=your-gemini-api-key
# Workspace directory (Claude CWD, file tree, terminals). Defaults to server directory if unset.
# WORKSPACE=/path/to/your/project
# Default permission mode when client does not send one (e.g. bypassPermissions = allow all for testing)
DEFAULT_PERMISSION_MODE=bypassPermissions
# Pi agent skills directory. ONLY skills from this folder are loaded. Default: <project-root>/skills
# SKILLS_DIR=/Users/yifanxu/machine_learning/LoVC/vibe-coding-everywhere_v2/skills
# AI Provider: "claude", "gemini", or "codex"
DEFAULT_PROVIDER=codex
# Codex app-server profile name from ~/.codex/config.toml [profiles.<name>].
# When set, server starts Codex with: codex app-server --profile <name>.
# CODEX_PROFILE=vibe-coding
# Gemini approval mode (default, auto_edit, plan)
DEFAULT_GEMINI_APPROVAL_MODE=auto_edit
# Claude output log directory. Log files are named claude-output-<timestamp>.log; one per server run.
# Defaults to <project-root>/logs (directory containing server.js) when unset.
# CLAUDE_OUTPUT_LOG=/path/to/logs

# Mock Claude: replay a log file instead of spawning the real CLI (for testing UI without API).
# MOCK_CLAUDE=1
# MOCK_CLAUDE=1 → app shows sequence picker; pick in UI or set:
# MOCK_CLAUDE_SEQUENCE=ask-two-questions-purpose-style
# MOCK_CLAUDE_LOG=/path/to/custom.log
# MOCK_CLAUDE_DELAY_MS=80
# MOCK_CLAUDE_EXIT_DELAY_MS=60000  (ms before emitting exit; keeps AskUserQuestion modal open until then, or until user confirms/cancels)

# Docker manager: enable /docker page and /api/docker/* API to list/start/stop containers. Opt-in for security.
# ENABLE_DOCKER_MANAGER=1

# Mobile (Expo) - optional override for default permission in app
# EXPO_PUBLIC_DEFAULT_PERMISSION_MODE=bypassPermissions
# EXPO_PUBLIC_SERVER_URL=http://YOUR_SERVER_IP:3456

# OpenZiti overlay network
# ZITI_PROXY_PORT - Port the reverse proxy listens on (Mac side). Default: 9443
# ZITI_PROXY_PORT=9443
# ZITI_SERVER_IDENTITY - Path to server identity JSON from Ziti enrollment. Default: .ziti/server.json
# ZITI_SERVER_IDENTITY=.ziti/server.json
# EXPO_PUBLIC_CONNECTION_MODE - Mobile connection mode: "direct", "ziti", "ziti-sdk". Auto-detected if unset.
# EXPO_PUBLIC_CONNECTION_MODE=ziti
# EXPO_PUBLIC_ZITI_PROXY_PORT - Port the Ziti proxy uses (mobile side, must match ZITI_PROXY_PORT). Default: 9443
# EXPO_PUBLIC_ZITI_PROXY_PORT=9443
